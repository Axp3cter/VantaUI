# Rokit / Wally tool + package caches. packages/ itself is vendored and
# tracked on purpose — the project must build without a wally install.
/.rokit
/DevPackages

# ProCMP personal config
pipeline/.pcmp.local.json

# luau-lsp / rojo scratch
sourcemap.json

# OS cruft
.DS_Store
Thumbs.db
